# MVP scope

Included: extension shell, configurable offsets, ETA scheduler, local settings, notifications, demo simulator, tests.

Excluded: private data upload, background GPS collection, and store publishing.
