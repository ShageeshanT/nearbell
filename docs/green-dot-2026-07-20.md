# Green-dot commit batch — 2026-07-20

18 meaningful commits pushed to `main` for the NearBell GitHub
contribution graph.

## Theme

Arrival UX polish — smart ETA formatting helpers. New utility
modules turn raw ETAs into stable, jitter-aware copy so arrival
alerts read smoothly instead of flickering every refresh tick.

## Helpers added

- `etaRoundToNearest` — bucket seconds to whole minutes
- `etaFormatCompact` — minutes-or-hours user-facing string
- `etaSingularPlural` — noun agreement helper
- `etaRoundingSnap` — snap to fixed minute buckets
- `etaJitterGuard` — decide if a delta is significant
- `etaDeltaSignificance` — tier + human label for ETA deltas
- `etaRefreshCadence` — adaptive refresh interval by distance
- `etaQuickLabel` — one-liner summary string

## Tests

- 8 new test files covering 28 new assertions
- Full suite: 271/271 passing
- Lint: 567 files clean
- Build: `dist/extension` rebuilt
